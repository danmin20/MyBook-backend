type Book {
  isbn: String!
  title: String!
  link: String
  image: String
  author: String!
  price: Int
  discount: Int
  publisher: String!
  description: String
}

type Query {
  books(term: String!): [Book]!
  book(id: String!): Book
}
